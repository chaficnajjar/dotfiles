#!/bin/bash

# Create output directory only if it does not already exist.
mkdir -p "$2";

cd "$1";

for jpg in *.jpg *.JPG *.jpeg *.JPEG; do
    jpegoptim --force --verbose --totals "$jpg" --dest="../$2";
done
